class Main {
    function void main() {
        var SnakeBombGame game;
        var boolean playing;
        let playing = true;
        while (playing) {
            let game = SnakeBombGame.new();
            do game.run();
            do game.dispose();
        }
        return;
    }
}

